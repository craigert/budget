# BudgetSparrow — agent rules

## Git push target

`origin` points to `github.com/craigert/budget`. Standard
`git push origin main` is correct. Cloudflare deploys from
craigert/budget via `.github/workflows/deploy.yml`.

There is no upstream mirror — the previous `Ravinia7212/BudgetSparrow`
remote was removed. Do not re-add it without asking.
